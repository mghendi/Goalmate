<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/parentView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SetupActivity">

    <include layout="@layout/layout_setup_one"
        android:id="@+id/setup_one"
        android:visibility="gone"/>
    <include layout="@layout/layout_setup_two"
        android:id="@+id/setup_two"
        android:visibility="gone"/>
    <include layout="@layout/layout_setup_three"
        android:id="@+id/setup_three"
        android:visibility="gone"/>
    <include layout="@layout/layout_setup_four"
        android:id="@+id/setup_four"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:layout_gravity="bottom|center_horizontal"
        android:textSize="16sp"
        android:textColor="?attr/colorAccent"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        app:srcCompat="@drawable/ic_next" />

</android.support.design.widget.CoordinatorLayout>